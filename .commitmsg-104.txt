feat(agent): agent dashboard — holdings, decision log, performance (Phase 10.4)

Render the 10.2/10.3 data richly on /app/agent. No new trading logic.

- Summary header: agent value (agent_cash + Σ qty×live price), total
  return $/% vs allocated, invested/cash split.
- Holdings table: symbol+name, qty, avg cost, live price (useQuotes),
  market value, weight %, unrealized P&L, and the watchdog's trailing
  stop level + cushion % (how far price sits above its stop).
- Decision log: agent_decisions as cards, newest-first, with color-coded
  action badges (buy / protective sell / rebalance / watchdog) and the
  plain-English rationale.
- Performance: new agent_snapshots table (migration 0007) recorded daily
  by the extended snapshot writer; reuses PortfolioValueChart with a new
  baseline prop (= allocated amount). SPY overlay deferred (TODO in UI).
- writer.server.ts gains an optional runSnapshots({ onlyUserId }) to
  scope to a single user (on-demand/test) without the whole-DB fan-out.

Verified end-to-end (real thinker + real writer, $50k balanced agent):
holdings reconcile (cash + invested = total, weights 100% of invested),
all holdings have seeded stops + cushion, decision log shows 6 buys + 1
rebalance, and agent_snapshots wrote a reconciling today row ($50000 snap
== $50000 UI). typecheck + build clean; no secrets in the client bundle.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
